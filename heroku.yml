build:
    docker:
        web: Dockerfile # Points to your Dockerfile for the build

run:
    web: gunicorn investment_chat_project.wsgi:application --bind 0.0.0.0:$PORT # Command to run your app

release:
    command:
        - python manage.py migrate # Run database migrations during the release phase
